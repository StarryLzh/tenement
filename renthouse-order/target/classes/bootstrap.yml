server:
  port: 9009
spring:
  application:
    name: renthouse-order
  profiles:
    active: mongodb
  cloud:
    config:
      uri: http://localhost:9010
      label: master
      name: renthouse
      profile: dev