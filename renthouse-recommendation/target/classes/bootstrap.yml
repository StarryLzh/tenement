server:
  port: 9002
spring:
  application:
    name: renthouse-recommendation
  profiles:
    active: bannerNginx,mongodb
  cloud:
    config:
      uri: http://localhost:9010
      label: master
      name: renthouse
      profile: dev